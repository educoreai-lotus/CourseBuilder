# Quick Guide: Test AI Course Creation

## 🎯 Two Easy Ways to Test

### **Option 1: Use Node.js Script (Recommended)**

```powershell
cd C:\Users\HP\Desktop\MainDevelopment_tamplates\backend
npm run test:ai-course
```

This script will:
- ✅ Create a course via API
- ✅ Show you the response with AI structure info
- ✅ Verify the course in database
- ✅ Show all topics/modules created by AI

---

### **Option 2: Use PowerShell Script**

```powershell
cd C:\Users\HP\Desktop\MainDevelopment_tamplates
.\test-ai-course-creation.ps1
```

---

### **Option 3: Manual PowerShell Command**

If you prefer to run the command manually:

```powershell
$body = '{"learner_id":"10000000-0000-0000-0000-000000000001","learner_name":"Alice Learner","learning_path":[{"topic_name":"JavaScript"}],"skills":["javascript"],"level":"beginner","duration":10,"sourceService":"test-script"}'
$headers = @{"Content-Type"="application/json"; "x-role"="service"; "x-user-role"="service"}
Invoke-RestMethod -Uri "http://localhost:3000/api/v1/courses/input" -Method Post -Body $body -Headers $headers
```

---

## 📋 Before Running

1. **Start your backend server:**
   ```powershell
   cd C:\Users\HP\Desktop\MainDevelopment_tamplates\backend
   npm run dev
   ```

2. **Make sure OpenAI API key is set:**
   - Check `backend/.env` file
   - Should have: `OPENAI_API_KEY=sk-...`

3. **Run the test script in a NEW terminal** (keep server running)

---

## ✅ What You Should See

After running the test, you should see:

```
✅ Course created successfully!

📋 Response:
{
  "status": "accepted",
  "course_id": "...",
  "structure": {
    "topics": 4,
    "modules": 8,
    "lessons": 15,
    "structureSource": "ai-generated"  ← This confirms AI was used!
  }
}

🤖 AI STRUCTURE INFORMATION
✅ AI successfully generated the structure!
```

---

## 🔍 Verify AI Was Used

After the course is created, verify it:

```powershell
cd backend
npm run check:ai-structure <course-id-from-response>
```

You should see:
- ✅ `Source: ai-generated`
- ✅ `Generated By: ai`
- ✅ Meaningful topic names (not "Topic 1", "Topic 2")

---

## 🚨 Troubleshooting

**If you get connection errors:**
- Make sure server is running: `npm run dev`
- Check server URL is `http://localhost:3000`

**If structure_source is "fallback":**
- Check OpenAI API key in `.env`
- Check server logs for OpenAI errors
- Verify OpenAI account has credits

---

## 📝 Quick Command Reference

```powershell
# Terminal 1: Start server
cd backend
npm run dev

# Terminal 2: Run test
cd backend
npm run test:ai-course

# Terminal 3: Verify AI structure
cd backend
npm run check:ai-structure <course-id>
```
